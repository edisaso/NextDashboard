/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;


module.exports = {
    images: {
      domains: ['i.imgur.com', 'static0.gamerantimages.com'], // Add the hostname here
    },
  };